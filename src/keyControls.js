// this should just make the keys nudge the ui controls, use nth-child to find correct things?
// maybe iterate over the graph with get-object-type?
// blocks of keys set detune to notes in key


module.exports = function (els, scale) {

}