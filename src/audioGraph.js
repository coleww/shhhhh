var getRoot = require('./root')

module.exports = function (rng) {
  var nodes = []
// always make like, N number of loopers, so key controls can control detune accordingly
// name loops something iterable
  var root = getRoot(rng)

  return nodes
}